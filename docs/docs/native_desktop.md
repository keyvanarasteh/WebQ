# Native Desktop Integrations

WebQ is transitioning into full Desktop Shell parity. Features included:
1. **Operating System Theme Watchers** (Tauri API triggers events on desktop theme shifts to bind Svelte values via `data-theme`).
2. **System Tray Integration** (Native Tray icons mapped into persistent OS environments enabling background caching without an active Window).
